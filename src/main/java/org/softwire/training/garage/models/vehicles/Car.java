package org.softwire.training.garage.models.vehicles;

public class Car extends Vehicle {
    public Car(String registrationNumber, boolean isLong, boolean isTall) {
        super(registrationNumber, isLong, isTall);
    }
}
